# 📜 Befehlsliste für **HelvetiCraft**

## 🔐 AuthMe

- `/register <Passwort> <Passwort>` – Registrieren
    
- `/login <Passwort>` – Anmelden
    
- `/changepassword <altesPasswort> <neuesPasswort>` – Passwort ändern (nur nach Login möglich)
    

---

## 🧰 ChestShop – Shop erstellen (kurz & einfach)

### 1️⃣ Truhe platzieren

Stelle **eine Truhe auf den Boden**. Sie enthält später die Shop-Items.

### 2️⃣ Schild platzieren

Platziere **ein Schild 1 Block von der Truhe entfernt**  
(z. B. direkt **über** der Truhe).

### 3️⃣ Schild beschriften

`Zeile 1: (leer lassen) Zeile 2: Menge Zeile 3: Preis Zeile 4: Item`

---

### 📝 Erklärung der Zeilen

**Zeile 1 – Besitzer**  
Leer lassen. Dein Name wird automatisch eingetragen.  
(Admins mit `ChestShop.admin` können andere Namen setzen.)

**Zeile 2 – Menge**  
Anzahl der Items pro Kauf/Verkauf  
Beispiel:

`64`

**Zeile 3 – Preis**

- **B** = Buy (Spieler kaufen vom Shop)
    
- **S** = Sell (Spieler verkaufen an den Shop)
    
- Trennung mit `:`
    

Beispiele:

- `B 5:5 S` – Kaufen für 5, Verkaufen für 5
    
- `B 5` – Nur kaufen für 5
    
- `S free` – Kostenlos verkaufen
    

**Zeile 4 – Item**

- `?` → Item wird automatisch aus der Truhe übernommen
    
- **oder** Schild mit Item in der Hand anklicken
    
- **oder** Item-Namen direkt eintragen
    

💡 Tipp: `/iteminfo` zeigt den genauen Namen des gehaltenen Items.

---

### ✅ Shop fertig

Der Shop ist aktiv, sobald das Schild korrekt erstellt wurde.

---

## 🧳 Essentials

- `/ec` oder `/enderchest` – Enderchest öffnen
    
- `/home` – Zum Home teleportieren
    
- `/home bed` – Zum Bett teleportieren
    
- `/sethome <Name>` – Home setzen
    
- `/delhome <Name>` – Home löschen
    

---

## 🏡 Claims (GriefPrevention)

- `/buyclaimblock <Anzahl>` – Claimblocks kaufen
    
- `/sellclaimblock <Anzahl>` – Unbenutzte Claimblocks verkaufen
    
- `/abandonclaim` – Aktuellen Claim aufgeben
    
- `/abandonallclaims` – Alle Claims aufgeben
    
- `/claimslist` – Eigene Claims anzeigen
    
- `/trust <Spielername>` – Spieler zum Claim hinzufügen
    

📌 Mit der **goldenen Schaufel** per Rechtsklick zwei diagonal gegenüberliegende Ecken markieren, um ein Grundstück zu erstellen.

---

## 🪑 Sit (GSit)

- `/sit` – Sitzen
    
- `/lay` – Liegen
    
- `/bellyflop` – Flach liegen
    
- `/crawl` – Kriechen
    

---

## 💰 Wirtschaft (HelvetiCraftPlugin)

- `/finance` – Eigene Finanzdaten anzeigen
    
- `/networth` – Vermögen aller Spieler & Serverwert
    
- `/save <Wert>` – Geld aufs Sparkonto legen
    
- `/pay <Spieler> <Wert>` – Geld senden
    
- `/sell <Spieler> <Wert>` – Item in der Hand anbieten
    
- `/sellaccept` – Verkaufsangebot annehmen
    
- `/selldecline` – Verkaufsangebot ablehnen
    
- `/convert` – Erz-Verkaufsmenü (Erze → CHF Spielgeld)
    
- `/initative` – Initiativen-Menü öffnen
    

---

## 🎭 Skins (SkinsRestorer)

- `/skin <Spielername>` – Skin eines anderen Spielers übernehmen